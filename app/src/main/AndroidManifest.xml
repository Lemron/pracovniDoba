<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jbory_000.pracovnidoba">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".zamestnanci" />
        <activity android:name=".zamestnavatel" />
        <activity android:name=".pridatZamestnance" />
        <activity android:name=".existujiciZamestnanec" />
        <activity android:name=".reportPrace" />
        <activity android:name=".pridatProjekt" />
        <activity android:name=".existujiciProjekt" />
        <activity android:name=".reportProjektu" />
        <activity android:name=".EmployeeDetails"/>
        <activity android:name=".DirectReports"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>